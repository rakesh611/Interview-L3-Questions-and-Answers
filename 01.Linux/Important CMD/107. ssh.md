# 107. ssh

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`ssh` is the OpenSSH client for encrypted remote login, command execution, tunneling, forwarding, jump-host access, and connection multiplexing.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** openssh-clients
- Check command availability:

```bash
command -v ssh 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Host-key verification authenticates the server and protects against man-in-the-middle attacks.
- User authentication can use passwords, public keys, certificates, Kerberos/GSSAPI, smart cards, or security keys.
- Client configuration is read from command-line options, user configuration, and system configuration with precedence rules.
- Local, remote, and dynamic forwarding have different traffic directions.
- Agent forwarding exposes authentication capability to the remote host and should be minimized.
- A successful TCP connection does not prove authentication or session setup will succeed.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `ssh user@server.example.com` | Open an interactive remote session. | Read-only |
| `ssh -p 2222 user@server.example.com` | Connect to a non-default port. | Read-only |
| `ssh -v user@server.example.com` | Enable one level of client debugging. | Read-only |
| `ssh -vvv user@server.example.com` | Enable maximum common client debugging. | Read-only |
| `ssh -i ~/.ssh/id_ed25519 user@server.example.com` | Select a private key. | Read-only |
| `ssh -J bastion.example.com user@internal.example.com` | Connect through a jump host. | Read-only |
| `ssh server.example.com 'uname -r; uptime'` | Run a remote command. | Read-only |
| `ssh -L 15432:db.internal:5432 bastion.example.com` | Create local port forwarding. | Network tunnel |
| `ssh -R 8080:localhost:80 server.example.com` | Create remote port forwarding. | Network tunnel |
| `ssh -D 1080 server.example.com` | Create a SOCKS proxy. | Network tunnel |
| `ssh -G server.example.com \| less` | Print effective client configuration. | Read-only |
| `ssh -o PreferredAuthentications=publickey -o PasswordAuthentication=no user@server` | Test public-key authentication only. | Read-only |

## 6. Detailed command examples

### 6.1 Open an interactive remote session.

**Risk level:** Read-only

```bash
ssh user@server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Connect to a non-default port.

**Risk level:** Read-only

```bash
ssh -p 2222 user@server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Enable one level of client debugging.

**Risk level:** Read-only

```bash
ssh -v user@server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Enable maximum common client debugging.

**Risk level:** Read-only

```bash
ssh -vvv user@server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Select a private key.

**Risk level:** Read-only

```bash
ssh -i ~/.ssh/id_ed25519 user@server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Connect through a jump host.

**Risk level:** Read-only

```bash
ssh -J bastion.example.com user@internal.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Run a remote command.

**Risk level:** Read-only

```bash
ssh server.example.com 'uname -r; uptime'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Create local port forwarding.

**Risk level:** Network tunnel

```bash
ssh -L 15432:db.internal:5432 bastion.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Create remote port forwarding.

**Risk level:** Network tunnel

```bash
ssh -R 8080:localhost:80 server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Create a SOCKS proxy.

**Risk level:** Network tunnel

```bash
ssh -D 1080 server.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Print effective client configuration.

**Risk level:** Read-only

```bash
ssh -G server.example.com | less
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Test public-key authentication only.

**Risk level:** Read-only

```bash
ssh -o PreferredAuthentications=publickey -o PasswordAuthentication=no user@server
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- Debug output shows configuration sources, address selection, key exchange, host-key checks, offered identities, and authentication methods.
- `Connection refused` usually means no listener or active rejection.
- `Connection timed out` points to routing, firewall, security-group, or path failure.
- `Permission denied` occurs after transport establishment and normally indicates authentication or authorization failure.
- A host-key mismatch must be investigated rather than bypassed blindly.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Do not use `StrictHostKeyChecking=no` as a routine workaround.
- Protect private keys with permissions and passphrases appropriate to their use.
- Avoid agent forwarding to hosts you do not fully trust.
- Port forwarding can bypass network-control assumptions and must follow policy.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Generate a lab key pair.
2. Connect with `-vvv` and identify every handshake phase.
3. Inspect effective configuration with `ssh -G`.
4. Test a jump host and one local forward.
5. Remove temporary keys and tunnels.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
