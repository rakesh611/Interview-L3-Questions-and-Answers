# 143. fsfreeze

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`fsfreeze` suspends new filesystem modifications and later resumes them. It is used to create crash-consistent storage snapshots when an external snapshot mechanism does not automatically coordinate filesystem freezing.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** util-linux
- Check availability and installed version before relying on an option:

```bash
command -v fsfreeze 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- Freezing drains or blocks filesystem modifications and can stall applications.
- The operation targets a mounted filesystem, not an arbitrary directory tree.
- The storage snapshot must be created while the filesystem is frozen, then the filesystem must be unfrozen immediately.
- Freezing does not guarantee application consistency for databases or distributed systems.
- Some snapshot tools already perform filesystem coordination, so double-freezing can be harmful.
- An administrative recovery path must remain available if the snapshot command hangs.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `findmnt -T /data -o TARGET,SOURCE,FSTYPE,OPTIONS` | Identify the exact filesystem. | Read-only |
| `sudo fsfreeze --freeze /data` | Freeze the mounted filesystem. | Immediate application-write suspension |
| `sudo fsfreeze --unfreeze /data` | Unfreeze the filesystem. | Recovery operation |
| `sudo fsfreeze -f /data` | Short form for freeze. | Immediate application-write suspension |
| `sudo fsfreeze -u /data` | Short form for unfreeze. | Recovery operation |
| `sudo lvcreate -s -L 10G -n data_snap /dev/vgdata/lvdata` | Create an LVM snapshot while frozen. | Storage snapshot creation |
| `sudo dmsetup status` | Inspect device-mapper state. | Read-only |
| `journalctl -k --since '-10 min'` | Review filesystem and block-layer messages. | Read-only |
| `ps -eo pid,stat,wchan:32,cmd \| awk '$2 ~ /^D/'` | Identify tasks blocked in uninterruptible sleep. | Read-only |
| `sync` | Request writeback before a planned freeze. | System-wide writeback |
| `systemd-run --scope -p TimeoutStopSec=30s /bin/bash` | Example of using an independently controlled administrative scope. | Read-only |

## 6. Detailed examples

### 6.1 Identify the exact filesystem.

**Operational classification:** Read-only

```bash
findmnt -T /data -o TARGET,SOURCE,FSTYPE,OPTIONS
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Freeze the mounted filesystem.

**Operational classification:** Immediate application-write suspension

```bash
sudo fsfreeze --freeze /data
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Unfreeze the filesystem.

**Operational classification:** Recovery operation

```bash
sudo fsfreeze --unfreeze /data
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Short form for freeze.

**Operational classification:** Immediate application-write suspension

```bash
sudo fsfreeze -f /data
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Short form for unfreeze.

**Operational classification:** Recovery operation

```bash
sudo fsfreeze -u /data
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Create an LVM snapshot while frozen.

**Operational classification:** Storage snapshot creation

```bash
sudo lvcreate -s -L 10G -n data_snap /dev/vgdata/lvdata
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Inspect device-mapper state.

**Operational classification:** Read-only

```bash
sudo dmsetup status
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Review filesystem and block-layer messages.

**Operational classification:** Read-only

```bash
journalctl -k --since '-10 min'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Identify tasks blocked in uninterruptible sleep.

**Operational classification:** Read-only

```bash
ps -eo pid,stat,wchan:32,cmd | awk '$2 ~ /^D/'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Request writeback before a planned freeze.

**Operational classification:** System-wide writeback

```bash
sync
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Example of using an independently controlled administrative scope.

**Operational classification:** Read-only

```bash
systemd-run --scope -p TimeoutStopSec=30s /bin/bash
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- The command normally produces no output on success.
- Application writes can block until unfreeze.
- A snapshot created before the freeze is not protected by the later freeze.
- A filesystem remaining frozen is an availability incident.
- Kernel logs can expose unsupported filesystem behavior or storage errors.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Prepare and test the snapshot and unfreeze commands before freezing.
- Keep a second root session outside the frozen filesystem.
- Minimize freeze duration to seconds.
- Use application quiescing for databases and stateful services when consistency requirements exceed crash consistency.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Create a disposable filesystem.
2. Start a loop writing timestamps.
3. Freeze it and observe blocking.
4. Create a snapshot.
5. Unfreeze immediately and validate the snapshot.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
