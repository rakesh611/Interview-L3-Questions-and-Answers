# 106. rpcinfo

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`rpcinfo` queries the ONC RPC port mapper and RPC services. It is commonly used to verify `rpcbind`, NFSv3 auxiliary services, program numbers, versions, protocols, and listening ports.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** rpcbind / nfs-utils, depending on distribution
- Check command availability:

```bash
command -v rpcinfo 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- NFSv4 primarily uses TCP port 2049 and needs fewer auxiliary RPC services than NFSv3.
- NFSv3 commonly depends on rpcbind, mountd, lock/stat services, and potentially dynamic ports.
- RPC program numbers identify services independently of TCP or UDP port.
- A service can be registered in rpcbind but still fail because of firewall, routing, or service health.
- Direct RPC probing is more specific than merely checking that a port is open.
- Firewalls are easier to manage when auxiliary NFS service ports are pinned.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `rpcinfo -p` | List local RPC programs registered with rpcbind. | Read-only |
| `rpcinfo -p nfsserver.example.com` | List remote registered RPC programs. | Read-only |
| `rpcinfo -s nfsserver.example.com` | Display a concise remote service list. | Read-only |
| `rpcinfo -t nfsserver.example.com nfs 3` | Probe NFS version 3 over TCP. | Read-only |
| `rpcinfo -u nfsserver.example.com nfs 3` | Probe NFS version 3 over UDP. | Read-only |
| `rpcinfo -t nfsserver.example.com mountd 3` | Probe mountd version 3 over TCP. | Read-only |
| `rpcinfo -n 2049 -t nfsserver.example.com nfs 4` | Probe a service on an explicit port. | Read-only |
| `rpcinfo -b mountd 3` | Broadcast for an RPC service on the local network; use only where permitted. | Network discovery |
| `systemctl status rpcbind` | Check the local rpcbind service. | Read-only |
| `ss -lntup \| grep -E ':(111\|2049)\b'` | Verify common RPC/NFS listeners. | Read-only |
| `sudo firewall-cmd --list-services --list-ports` | Inspect firewall exposure. | Read-only |

## 6. Detailed command examples

### 6.1 List local RPC programs registered with rpcbind.

**Risk level:** Read-only

```bash
rpcinfo -p
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 List remote registered RPC programs.

**Risk level:** Read-only

```bash
rpcinfo -p nfsserver.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Display a concise remote service list.

**Risk level:** Read-only

```bash
rpcinfo -s nfsserver.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Probe NFS version 3 over TCP.

**Risk level:** Read-only

```bash
rpcinfo -t nfsserver.example.com nfs 3
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Probe NFS version 3 over UDP.

**Risk level:** Read-only

```bash
rpcinfo -u nfsserver.example.com nfs 3
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Probe mountd version 3 over TCP.

**Risk level:** Read-only

```bash
rpcinfo -t nfsserver.example.com mountd 3
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Probe a service on an explicit port.

**Risk level:** Read-only

```bash
rpcinfo -n 2049 -t nfsserver.example.com nfs 4
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Broadcast for an RPC service on the local network; use only where permitted.

**Risk level:** Network discovery

```bash
rpcinfo -b mountd 3
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Check the local rpcbind service.

**Risk level:** Read-only

```bash
systemctl status rpcbind
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Verify common RPC/NFS listeners.

**Risk level:** Read-only

```bash
ss -lntup | grep -E ':(111|2049)\b'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Inspect firewall exposure.

**Risk level:** Read-only

```bash
sudo firewall-cmd --list-services --list-ports
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- Program `100000` is normally portmapper/rpcbind.
- Program `100003` is normally NFS.
- Program `100005` is normally mountd.
- Multiple entries can represent TCP/UDP and several protocol versions.
- `program not registered` means rpcbind has no matching registration, not necessarily that the host is unreachable.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Do not expose rpcbind and NFS auxiliary services to untrusted networks.
- Broadcast probing can trigger security monitoring.
- Use name resolution and direct IP tests to separate DNS from RPC failure.
- Match firewall rules to the actual NFS version and pinned service ports.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Start a lab NFS server.
2. List local and remote RPC registrations.
3. Probe NFS versions 3 and 4.
4. Stop one auxiliary service and observe the difference.
5. Restore the service and verify firewall state.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
