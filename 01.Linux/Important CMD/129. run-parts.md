# 129. run-parts

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`run-parts` executes executable files found in a directory according to implementation-specific filename rules. It is commonly used by periodic cron directories and maintenance frameworks.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** cronie or debianutils, depending on distribution
- Check availability and installed version before relying on an option:

```bash
command -v run-parts 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- Filename acceptance rules differ across distributions and implementations.
- Scripts must be executable and usually should not contain dots or disallowed characters under default rules.
- Execution order is normally lexical according to the implementation's rules.
- The environment is minimal when run from cron.
- One failing script may or may not stop later scripts depending on options.
- The directory must not be writable by untrusted users.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `run-parts --test /etc/cron.daily` | List scripts that would run without executing them. | Read-only |
| `run-parts --list /etc/cron.daily` | List accepted names where supported. | Read-only |
| `sudo run-parts --verbose /etc/cron.daily` | Execute accepted scripts verbosely. | Scheduled-script execution |
| `sudo run-parts --report /etc/cron.daily` | Report only scripts producing output. | Scheduled-script execution |
| `run-parts --reverse --test /etc/cron.daily` | Preview reverse execution order. | Read-only |
| `run-parts --regex='^[A-Za-z0-9_-]+$' --test /opt/jobs` | Use an explicit filename pattern where supported. | Read-only |
| `run-parts --arg='--dry-run' --test /opt/jobs` | Preview passing an argument where supported. | Read-only |
| `find /etc/cron.daily -maxdepth 1 -type f -printf '%f %m %u:%g\n' \| sort` | Inspect names, modes, and ownership. | Read-only |
| `systemctl cat cron.service crond.service 2>/dev/null` | Inspect the scheduler service. | Read-only |
| `journalctl -u crond --since today` | Review cron activity on systems using crond. | Read-only |

## 6. Detailed examples

### 6.1 List scripts that would run without executing them.

**Operational classification:** Read-only

```bash
run-parts --test /etc/cron.daily
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 List accepted names where supported.

**Operational classification:** Read-only

```bash
run-parts --list /etc/cron.daily
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Execute accepted scripts verbosely.

**Operational classification:** Scheduled-script execution

```bash
sudo run-parts --verbose /etc/cron.daily
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Report only scripts producing output.

**Operational classification:** Scheduled-script execution

```bash
sudo run-parts --report /etc/cron.daily
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Preview reverse execution order.

**Operational classification:** Read-only

```bash
run-parts --reverse --test /etc/cron.daily
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Use an explicit filename pattern where supported.

**Operational classification:** Read-only

```bash
run-parts --regex='^[A-Za-z0-9_-]+$' --test /opt/jobs
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Preview passing an argument where supported.

**Operational classification:** Read-only

```bash
run-parts --arg='--dry-run' --test /opt/jobs
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Inspect names, modes, and ownership.

**Operational classification:** Read-only

```bash
find /etc/cron.daily -maxdepth 1 -type f -printf '%f %m %u:%g\n' | sort
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Inspect the scheduler service.

**Operational classification:** Read-only

```bash
systemctl cat cron.service crond.service 2>/dev/null
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Review cron activity on systems using crond.

**Operational classification:** Read-only

```bash
journalctl -u crond --since today
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- A script missing from `--test` usually has an unacceptable name, mode, or file type.
- Successful manual execution does not prove it will work under cron's minimal environment.
- Lexical order can be deliberately controlled with numeric prefixes.
- Output handling depends on the caller, mail configuration, and service logging.
- Implementation differences should be resolved with `run-parts --help` and the local manual.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Always preview the directory before execution.
- Do not run production periodic directories manually during peak load without understanding every script.
- Use absolute command paths and an explicit environment in scripts.
- Reject writable or untrusted script directories.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Create three numbered test scripts.
2. Preview ordering.
3. Add a filename containing a dot and observe acceptance.
4. Run with a minimal environment.
5. Capture return codes and output.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
