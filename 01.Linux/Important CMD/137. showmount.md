# 137. showmount

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`showmount` queries an NFS server's mount daemon for export and client-mount information. It is mainly relevant to NFSv2/v3 and may not accurately represent pure NFSv4 environments.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** nfs-utils
- Check availability and installed version before relying on an option:

```bash
command -v showmount 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- The tool depends on rpcbind/mountd RPC services.
- NFSv4 can operate without the same mountd discovery path.
- Export visibility does not prove filesystem permissions or successful data access.
- Client lists are historical or implementation-specific and are not authoritative session inventories.
- Firewalls can allow NFS port 2049 while blocking rpcbind or mountd.
- Reverse DNS can affect displayed client names and command delay.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `showmount -e nfsserver.example.com` | List exports visible through mountd. | Read-only |
| `showmount -a nfsserver.example.com` | List client-to-directory mount records. | Read-only |
| `showmount -d nfsserver.example.com` | List directories reportedly mounted by clients. | Read-only |
| `showmount --no-headers -e nfsserver.example.com` | Produce simpler output where supported. | Read-only |
| `rpcinfo -p nfsserver.example.com` | Verify rpcbind and mountd registration. | Read-only |
| `rpcinfo -t nfsserver.example.com mountd 3` | Probe mountd directly. | Read-only |
| `nc -vz nfsserver.example.com 2049` | Test the common NFS TCP port. | Read-only |
| `mount -t nfs4 nfsserver.example.com:/ /mnt/test` | Test the NFSv4 pseudo-root in a lab. | Temporary mount |
| `nfsstat -m` | Inspect effective client mount options. | Read-only |
| `sudo tcpdump -ni any 'host 192.0.2.20 and (port 111 or port 2049)'` | Observe RPC/NFS traffic. | Read-only |

## 6. Detailed examples

### 6.1 List exports visible through mountd.

**Operational classification:** Read-only

```bash
showmount -e nfsserver.example.com
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 List client-to-directory mount records.

**Operational classification:** Read-only

```bash
showmount -a nfsserver.example.com
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 List directories reportedly mounted by clients.

**Operational classification:** Read-only

```bash
showmount -d nfsserver.example.com
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Produce simpler output where supported.

**Operational classification:** Read-only

```bash
showmount --no-headers -e nfsserver.example.com
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Verify rpcbind and mountd registration.

**Operational classification:** Read-only

```bash
rpcinfo -p nfsserver.example.com
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Probe mountd directly.

**Operational classification:** Read-only

```bash
rpcinfo -t nfsserver.example.com mountd 3
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Test the common NFS TCP port.

**Operational classification:** Read-only

```bash
nc -vz nfsserver.example.com 2049
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Test the NFSv4 pseudo-root in a lab.

**Operational classification:** Temporary mount

```bash
mount -t nfs4 nfsserver.example.com:/ /mnt/test
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Inspect effective client mount options.

**Operational classification:** Read-only

```bash
nfsstat -m
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Observe RPC/NFS traffic.

**Operational classification:** Read-only

```bash
sudo tcpdump -ni any 'host 192.0.2.20 and (port 111 or port 2049)'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- An empty export list can mean NFSv4-only service rather than no usable exports.
- `RPC: Program not registered` indicates absent or unreachable mountd registration.
- A visible export can still deny a client because of selector mismatch or filesystem security.
- Client names can be stale or DNS-derived.
- Use `exportfs -v` on the server as a stronger active-export source.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Do not expose mountd information to untrusted networks.
- Do not treat client lists as complete audit evidence.
- Use direct NFSv4 mount testing for NFSv4-only systems.
- Coordinate probing with network-security policy.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Configure a lab NFSv3 export.
2. Compare `showmount -e` with `exportfs -v`.
3. Switch to NFSv4-only exposure.
4. Observe the difference.
5. Capture the RPC queries.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
