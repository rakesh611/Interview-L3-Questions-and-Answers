# 82. ethtool

> **Audience:** Linux L2/L3 administrators, performance engineers, and production support teams.

## 1. Overview

Queries and configures Ethernet link, driver, offload, ring, channel, coalescing, pause, statistics, and self-test settings.

## 2. Package and availability

- **Package/interface:** `ethtool`
- Verify with `command -v COMMAND`, `rpm -q PACKAGE`, or the distribution equivalent.
- Use `man COMMAND` and `COMMAND --help` on the target host because options can vary by release.

## 3. Important concepts

- Driver counters are more specific than generic interface counters.
- Offloads improve throughput but affect capture appearance.
- Ring, channel, and coalescing changes affect drops, CPU, and latency.

## 4. Important commands

| Command | Purpose |
|---|---|
| `sudo ethtool eth0` | Show speed, duplex, negotiation, and link |
| `sudo ethtool -i eth0` | Show driver and firmware |
| `sudo ethtool -S eth0` | Show NIC statistics |
| `sudo ethtool -k eth0` | Show offloads |
| `sudo ethtool -K eth0 gro off gso off tso off` | Disable selected offloads temporarily |
| `sudo ethtool -g eth0` | Show ring sizes |
| `sudo ethtool -G eth0 rx 4096 tx 4096` | Set ring sizes |
| `sudo ethtool -l eth0` | Show channels |
| `sudo ethtool -L eth0 combined 8` | Set channels |
| `sudo ethtool -c eth0` | Show coalescing |
| `sudo ethtool --show-pause eth0` | Show pause frames |
| `sudo ethtool -t eth0 online` | Run supported online tests |

## 5. Detailed examples

### 5.1 Show speed, duplex, negotiation, and link

```bash
sudo ethtool eth0
```

**Use:** Show speed, duplex, negotiation, and link. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.2 Show driver and firmware

```bash
sudo ethtool -i eth0
```

**Use:** Show driver and firmware. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.3 Show NIC statistics

```bash
sudo ethtool -S eth0
```

**Use:** Show NIC statistics. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.4 Show offloads

```bash
sudo ethtool -k eth0
```

**Use:** Show offloads. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.5 Disable selected offloads temporarily

```bash
sudo ethtool -K eth0 gro off gso off tso off
```

**Use:** Disable selected offloads temporarily. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.6 Show ring sizes

```bash
sudo ethtool -g eth0
```

**Use:** Show ring sizes. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.7 Set ring sizes

```bash
sudo ethtool -G eth0 rx 4096 tx 4096
```

**Use:** Set ring sizes. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.8 Show channels

```bash
sudo ethtool -l eth0
```

**Use:** Show channels. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.9 Set channels

```bash
sudo ethtool -L eth0 combined 8
```

**Use:** Set channels. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.10 Show coalescing

```bash
sudo ethtool -c eth0
```

**Use:** Show coalescing. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.11 Show pause frames

```bash
sudo ethtool --show-pause eth0
```

**Use:** Show pause frames. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.12 Run supported online tests

```bash
sudo ethtool -t eth0 online
```

**Use:** Run supported online tests. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

## 6. Output interpretation checklist

- Confirm that the command targeted the intended PID, device, interface, filesystem, user, service, or namespace.
- Distinguish cumulative counters from interval rates and point-in-time snapshots.
- Compare the result with a known baseline and with application symptoms.
- Correlate with `journalctl`, kernel logs, service logs, and neighboring subsystem tools.
- Treat missing output as a possible permission, namespace, package, or version issue before concluding that no problem exists.

## 7. Production troubleshooting workflow

1. Record the incident time, hostname, kernel version, and affected service.
2. Run read-only inventory and status commands first.
3. For changing metrics, collect at least five interval samples.
4. Identify the narrowest affected object and avoid host-wide changes where possible.
5. Form one hypothesis and choose one measurable corrective action.
6. Capture the original value or configuration and prepare rollback.
7. Apply the change through the owning manager, such as systemd, NetworkManager, LVM, or the service configuration.
8. Repeat the same measurements and compare application latency, errors, throughput, and saturation.
9. Roll back if the intended service metric does not improve.
10. Document root cause, evidence, corrective action, and prevention.

## 8. Production cautions

- Link changes can disconnect the host.
- Record original settings.
- Persist tested values through the network manager.
- Commands that resize, repair, create, remove, flush, signal, reload, or reconfigure can cause outage or data loss.
- Replace example names and addresses only after validating ownership and dependencies.
- Use change control, verified backups, console access, and a rollback plan for storage, network, time, scheduler, and authentication changes.

## 9. Quick lab

1. Run the first three inspection commands on a non-production host.
2. Save the output and explain every major field.
3. Trigger one safe, controlled state change if the tool supports it.
4. Re-run the inspection and compare the output.
5. Restore the original state and document the rollback.

## 10. L3 interview review

- Explain what kernel or service interface the command reads or changes.
- Identify whether the output is a snapshot, cumulative counter, or interval rate.
- Name two misleading interpretations and the additional evidence needed.
- Explain persistence, configuration ownership, and rollback.
- Describe the safest escalation evidence bundle for vendor support.
