# 132. /sys/block/DEVICE/queue/scheduler

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`/sys/block/DEVICE/queue/scheduler` displays the available block I/O schedulers for a device and identifies the active scheduler with brackets. A privileged write selects a scheduler at runtime.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** Kernel sysfs block-queue interface
- Check availability and installed version before relying on an option:

```bash
command -v  2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- Scheduler availability depends on kernel, device type, queue model, and loaded modules.
- Common schedulers include `none`, `mq-deadline`, `bfq`, and `kyber` depending on the platform.
- Virtual, NVMe, SAN, multipath, and rotational devices have different workload considerations.
- The visible device can be a partition or device-mapper layer whose scheduling occurs on underlying devices.
- A scheduler change is runtime-only unless persisted by TuneD, udev, kernel parameters, or another manager.
- Performance must be judged by latency percentiles, throughput, queue depth, CPU use, and application behavior.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `cat /sys/block/sda/queue/scheduler` | Display available and active schedulers. | Read-only |
| `cat /sys/block/nvme0n1/queue/scheduler` | Inspect an NVMe device. | Read-only |
| `echo mq-deadline \| sudo tee /sys/block/sda/queue/scheduler` | Select `mq-deadline` at runtime. | Runtime storage tuning |
| `echo none \| sudo tee /sys/block/nvme0n1/queue/scheduler` | Select `none` at runtime. | Runtime storage tuning |
| `lsblk -d -o NAME,TYPE,ROTA,TRAN,SCHED,HCTL,MODEL,SERIAL` | Inventory device type and scheduler. | Read-only |
| `udevadm info --query=all --name=/dev/sda` | Collect stable device properties. | Read-only |
| `cat /sys/block/sda/queue/rotational` | Check the kernel rotational flag. | Read-only |
| `cat /sys/block/sda/queue/nr_requests` | Inspect queue request depth. | Read-only |
| `iostat -xz 1 10` | Measure latency, utilization, and queue behavior. | Read-only |
| `find /sys/block/dm-0/slaves -maxdepth 1 -type l -printf '%f\n'` | Identify underlying devices for a mapper device. | Read-only |
| `tuned-adm active; tuned-adm profile_info` | Check whether TuneD owns storage settings. | Read-only |
| `grep -R 'scheduler' /etc/udev/rules.d /usr/lib/udev/rules.d /etc/tuned 2>/dev/null` | Find persistence rules. | Read-only |

## 6. Detailed examples

### 6.1 Display available and active schedulers.

**Operational classification:** Read-only

```bash
cat /sys/block/sda/queue/scheduler
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Inspect an NVMe device.

**Operational classification:** Read-only

```bash
cat /sys/block/nvme0n1/queue/scheduler
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Select `mq-deadline` at runtime.

**Operational classification:** Runtime storage tuning

```bash
echo mq-deadline | sudo tee /sys/block/sda/queue/scheduler
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Select `none` at runtime.

**Operational classification:** Runtime storage tuning

```bash
echo none | sudo tee /sys/block/nvme0n1/queue/scheduler
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Inventory device type and scheduler.

**Operational classification:** Read-only

```bash
lsblk -d -o NAME,TYPE,ROTA,TRAN,SCHED,HCTL,MODEL,SERIAL
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Collect stable device properties.

**Operational classification:** Read-only

```bash
udevadm info --query=all --name=/dev/sda
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Check the kernel rotational flag.

**Operational classification:** Read-only

```bash
cat /sys/block/sda/queue/rotational
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Inspect queue request depth.

**Operational classification:** Read-only

```bash
cat /sys/block/sda/queue/nr_requests
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Measure latency, utilization, and queue behavior.

**Operational classification:** Read-only

```bash
iostat -xz 1 10
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Identify underlying devices for a mapper device.

**Operational classification:** Read-only

```bash
find /sys/block/dm-0/slaves -maxdepth 1 -type l -printf '%f\n'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Check whether TuneD owns storage settings.

**Operational classification:** Read-only

```bash
tuned-adm active; tuned-adm profile_info
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.12 Find persistence rules.

**Operational classification:** Read-only

```bash
grep -R 'scheduler' /etc/udev/rules.d /usr/lib/udev/rules.d /etc/tuned 2>/dev/null
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- Example output `none [mq-deadline] bfq` means `mq-deadline` is active.
- `none` does not mean no queue exists; it means no additional scheduler policy beyond the multiqueue block layer.
- A blank or missing interface may indicate a device type without a selectable scheduler at that layer.
- Changes on a partition path should normally be applied to the whole block device.
- An average-latency improvement can hide worse tail latency.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Benchmark with a representative workload and record the original value.
- Do not tune the mapper layer while ignoring the underlying physical paths.
- Avoid global udev rules that accidentally affect boot, SAN, removable, or virtual devices.
- Coordinate with database, storage-array, and virtualization vendor recommendations.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Record topology and active scheduler.
2. Run a controlled fio baseline.
3. Change one scheduler.
4. Repeat the exact workload.
5. Restore the original scheduler and compare percentile latency.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
