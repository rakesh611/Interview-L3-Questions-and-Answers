# 124. dnf, rpm, and yum

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`dnf` and `yum` perform repository-aware package management, dependency solving, transactions, updates, and history operations. `rpm` manages and queries individual RPM packages and the local RPM database at a lower level.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** dnf, rpm; `yum` may be a compatibility command
- Check command availability:

```bash
command -v dnf 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- `dnf` resolves dependencies and repository metadata; `rpm` does not normally resolve dependencies.
- A package name, NEVRA, and RPM filename are different identifiers.
- Installed files can be verified against RPM metadata, but changed configuration files may be expected.
- Transaction history helps identify what changed and supports limited undo/rollback behavior.
- Module streams, repositories, version locks, exclusions, and vendor content policies affect available versions.
- Downgrading packages does not automatically reverse data-format or database migrations.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `dnf repolist --all` | List enabled and disabled repositories. | Read-only |
| `dnf list installed` | List installed packages. | Read-only |
| `dnf info bash` | Show package information. | Read-only |
| `dnf provides '/usr/bin/semanage'` | Find the package providing a file. | Read-only |
| `sudo dnf install package-name` | Install a package with dependencies. | Package change |
| `sudo dnf upgrade --refresh` | Refresh metadata and update packages. | Broad package change |
| `sudo dnf remove package-name` | Remove a package and resolved dependencies. | Package removal |
| `dnf check-update` | Check for updates without installing them. | Read-only |
| `dnf history list` | List package transactions. | Read-only |
| `dnf history info 42` | Inspect one transaction. | Read-only |
| `sudo dnf history undo 42` | Attempt to reverse one transaction. | Package rollback attempt |
| `rpm -q bash` | Query whether a package is installed. | Read-only |
| `rpm -qi bash` | Show installed-package information. | Read-only |
| `rpm -ql bash` | List files owned by a package. | Read-only |
| `rpm -qf /usr/bin/bash` | Find the installed package owning a file. | Read-only |
| `rpm -V bash` | Verify installed files against RPM metadata. | Read-only |
| `rpm -K package.rpm` | Verify an RPM signature and digest. | Read-only |
| `rpm -qp --requires package.rpm` | Inspect requirements of an RPM file. | Read-only |
| `sudo rpm --rebuilddb` | Rebuild RPM database indexes only when justified. | Database maintenance |
| `yum install package-name` | Use the yum-compatible interface where provided. | Package change |

## 6. Detailed command examples

### 6.1 List enabled and disabled repositories.

**Risk level:** Read-only

```bash
dnf repolist --all
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 List installed packages.

**Risk level:** Read-only

```bash
dnf list installed
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Show package information.

**Risk level:** Read-only

```bash
dnf info bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Find the package providing a file.

**Risk level:** Read-only

```bash
dnf provides '/usr/bin/semanage'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Install a package with dependencies.

**Risk level:** Package change

```bash
sudo dnf install package-name
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Refresh metadata and update packages.

**Risk level:** Broad package change

```bash
sudo dnf upgrade --refresh
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Remove a package and resolved dependencies.

**Risk level:** Package removal

```bash
sudo dnf remove package-name
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Check for updates without installing them.

**Risk level:** Read-only

```bash
dnf check-update
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 List package transactions.

**Risk level:** Read-only

```bash
dnf history list
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Inspect one transaction.

**Risk level:** Read-only

```bash
dnf history info 42
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Attempt to reverse one transaction.

**Risk level:** Package rollback attempt

```bash
sudo dnf history undo 42
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Query whether a package is installed.

**Risk level:** Read-only

```bash
rpm -q bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.13 Show installed-package information.

**Risk level:** Read-only

```bash
rpm -qi bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.14 List files owned by a package.

**Risk level:** Read-only

```bash
rpm -ql bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.15 Find the installed package owning a file.

**Risk level:** Read-only

```bash
rpm -qf /usr/bin/bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.16 Verify installed files against RPM metadata.

**Risk level:** Read-only

```bash
rpm -V bash
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.17 Verify an RPM signature and digest.

**Risk level:** Read-only

```bash
rpm -K package.rpm
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.18 Inspect requirements of an RPM file.

**Risk level:** Read-only

```bash
rpm -qp --requires package.rpm
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.19 Rebuild RPM database indexes only when justified.

**Risk level:** Database maintenance

```bash
sudo rpm --rebuilddb
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.20 Use the yum-compatible interface where provided.

**Risk level:** Package change

```bash
yum install package-name
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- NEVRA means Name, Epoch, Version, Release, and Architecture.
- RPM verification codes identify differences in size, mode, digest, device, link, owner, group, or time.
- `dnf history undo` can fail when old packages are unavailable or intervening dependencies changed.
- Repository priority and enabled content determine the selected candidate.
- A package installation success does not guarantee its service is enabled, configured, or healthy.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Review the transaction summary before accepting installation or removal.
- Do not use `rpm --nodeps` or `--force` as a routine dependency workaround.
- Take application and data backups before major upgrades or downgrades.
- Use approved repositories and verify signatures.
- Coordinate kernel, bootloader, database, and cluster package changes through change control.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Query an installed package.
2. Find which package owns a command.
3. Download an RPM without installing it.
4. Inspect signature, files, requirements, and scripts.
5. Install and remove a harmless lab package through `dnf` and review history.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
