# 113. aureport

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`aureport` creates summary reports from Linux Audit logs, including authentication, login, executable, file, syscall, user, anomaly, AVC, and event reports.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** audit
- Check command availability:

```bash
command -v aureport 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Reports summarize audit evidence; use `ausearch` to inspect individual events.
- Counts depend on the selected time window and available log retention.
- Failed and successful events should be separated where operationally important.
- Summary data can identify trends but cannot replace event-level root-cause analysis.
- Input can come from the default audit logs or standard input.
- Time, hostname, and log-rotation boundaries matter.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `sudo aureport` | Display the general summary report. | Read-only |
| `sudo aureport -au` | Report authentication events. | Read-only |
| `sudo aureport -l` | Report login events. | Read-only |
| `sudo aureport -x` | Report executable events. | Read-only |
| `sudo aureport -f` | Report file events. | Read-only |
| `sudo aureport -s` | Report system-call events. | Read-only |
| `sudo aureport -u` | Report user events. | Read-only |
| `sudo aureport -a` | Report AVC events. | Read-only |
| `sudo aureport --failed` | Restrict to failed events. | Read-only |
| `sudo aureport --success` | Restrict to successful events. | Read-only |
| `sudo aureport -ts today -te now -i` | Create an interpreted report for today. | Read-only |
| `sudo aureport --summary -x` | Summarize executable activity. | Read-only |
| `cat /var/log/audit/audit.log.1 \| sudo aureport -au --stdin` | Report from an archived input stream. | Read-only |

## 6. Detailed command examples

### 6.1 Display the general summary report.

**Risk level:** Read-only

```bash
sudo aureport
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Report authentication events.

**Risk level:** Read-only

```bash
sudo aureport -au
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Report login events.

**Risk level:** Read-only

```bash
sudo aureport -l
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Report executable events.

**Risk level:** Read-only

```bash
sudo aureport -x
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Report file events.

**Risk level:** Read-only

```bash
sudo aureport -f
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Report system-call events.

**Risk level:** Read-only

```bash
sudo aureport -s
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Report user events.

**Risk level:** Read-only

```bash
sudo aureport -u
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Report AVC events.

**Risk level:** Read-only

```bash
sudo aureport -a
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Restrict to failed events.

**Risk level:** Read-only

```bash
sudo aureport --failed
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Restrict to successful events.

**Risk level:** Read-only

```bash
sudo aureport --success
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Create an interpreted report for today.

**Risk level:** Read-only

```bash
sudo aureport -ts today -te now -i
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Summarize executable activity.

**Risk level:** Read-only

```bash
sudo aureport --summary -x
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.13 Report from an archived input stream.

**Risk level:** Read-only

```bash
cat /var/log/audit/audit.log.1 | sudo aureport -au --stdin
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- Use report event numbers to pivot back to detailed `ausearch -a EVENT_ID` output.
- High failed-login counts need source, account, service, and time-pattern analysis.
- Executable reports show audit-visible execution, not necessarily every command unless rules collect it.
- AVC reports summarize SELinux denials recorded in the audit stream.
- A low count can reflect limited rules rather than low activity.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Do not present summary counts without the time range and log sources.
- Protect reports because they expose account and system activity.
- Avoid assuming every authentication failure is malicious.
- Preserve raw logs for forensic validation.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Generate several test authentication and file events.
2. Create summary and failed-only reports.
3. Select an event number.
4. Open the full event with `ausearch`.
5. Document the difference between summary and evidence.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
