# 130. crontab

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`crontab` installs, lists, edits, or removes per-user cron schedules. Cron starts commands at calendar-defined times using a limited environment.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** cronie / cron
- Check availability and installed version before relying on an option:

```bash
command -v crontab 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- A user crontab has five time fields followed by the command.
- System crontab files commonly include an additional user field.
- Cron's timezone, environment, PATH, shell, and daylight-saving behavior must be understood.
- Percent characters in commands can have special meaning in some cron implementations.
- Overlapping jobs require explicit locking or idempotent design.
- Systemd timers can provide stronger dependency, logging, and missed-run controls.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `crontab -l` | List the current user's crontab. | Read-only |
| `crontab -e` | Edit the current user's crontab. | Schedule change |
| `sudo crontab -u appuser -l` | List another user's crontab. | Read-only |
| `sudo crontab -u appuser -e` | Edit another user's crontab. | Schedule change |
| `crontab -r` | Remove the current user's crontab without prompting. | Destructive schedule removal |
| `crontab -i -r` | Prompt before removing the crontab. | Schedule removal |
| `printf '*/5 * * * * /usr/local/sbin/job.sh\n' \| crontab -` | Replace the entire user crontab from standard input. | Broad schedule replacement |
| `crontab -l > /root/appuser.crontab.backup` | Back up a crontab. | Read-only |
| `sudo crond -T 2>/dev/null \|\| true` | Test configuration where the installed daemon supports it. | Read-only |
| `systemctl status crond cron 2>/dev/null` | Check the cron daemon. | Read-only |
| `journalctl -u crond --since '-1 hour'` | Review cron logs. | Read-only |
| `flock -n /run/job.lock /usr/local/sbin/job.sh` | Prevent overlapping job instances. | Read-only |

## 6. Detailed examples

### 6.1 List the current user's crontab.

**Operational classification:** Read-only

```bash
crontab -l
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Edit the current user's crontab.

**Operational classification:** Schedule change

```bash
crontab -e
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 List another user's crontab.

**Operational classification:** Read-only

```bash
sudo crontab -u appuser -l
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Edit another user's crontab.

**Operational classification:** Schedule change

```bash
sudo crontab -u appuser -e
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Remove the current user's crontab without prompting.

**Operational classification:** Destructive schedule removal

```bash
crontab -r
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Prompt before removing the crontab.

**Operational classification:** Schedule removal

```bash
crontab -i -r
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Replace the entire user crontab from standard input.

**Operational classification:** Broad schedule replacement

```bash
printf '*/5 * * * * /usr/local/sbin/job.sh\n' | crontab -
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Back up a crontab.

**Operational classification:** Read-only

```bash
crontab -l > /root/appuser.crontab.backup
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Test configuration where the installed daemon supports it.

**Operational classification:** Read-only

```bash
sudo crond -T 2>/dev/null || true
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Check the cron daemon.

**Operational classification:** Read-only

```bash
systemctl status crond cron 2>/dev/null
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Review cron logs.

**Operational classification:** Read-only

```bash
journalctl -u crond --since '-1 hour'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.12 Prevent overlapping job instances.

**Operational classification:** Read-only

```bash
flock -n /run/job.lock /usr/local/sbin/job.sh
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- `*/5` means every five units in that field, not five minutes after the previous completion.
- A job can be scheduled correctly but fail because of PATH, permissions, SELinux, working directory, or missing environment variables.
- Cron normally does not catch up missed jobs after downtime unless another mechanism provides that behavior.
- Mail output depends on `MAILTO` and a working local mail path.
- A job with no output can still fail; log the exit code and critical results.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Back up before using commands that replace or remove the entire crontab.
- Never store passwords or tokens directly in crontab command lines.
- Use locking, timeouts, and bounded retries for production jobs.
- Prefer systemd timers when dependencies, persistence, resource controls, and journal integration are important.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Create a job that logs a unique marker.
2. Set explicit SHELL and PATH.
3. Test output and environment.
4. Add `flock` to prevent overlap.
5. Remove the lab schedule safely.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
