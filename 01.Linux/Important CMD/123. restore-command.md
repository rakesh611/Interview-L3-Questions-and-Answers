# 123. restore-command — interpreted as restorecon

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

The requested name `restore-command` is interpreted here as the SELinux `restorecon` command because it appears beside `matchpathcon` and other SELinux metadata tools. `restorecon` restores a path's default SELinux context according to the active file-context policy.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** policycoreutils
- Check command availability:

```bash
command -v restore-command 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- `restorecon` applies the policy-defined context; it does not invent a context from the parent directory alone.
- Persistent custom mappings should be created with `semanage fcontext`.
- `chcon` is normally temporary and can be overwritten by `restorecon` or a full relabel.
- Recursive relabeling can touch many files and generate significant metadata I/O.
- The `-n` option previews changes without applying them.
- Relabeling corrects context mismatches but does not automatically solve every SELinux denial.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `restorecon /var/www/html/index.html` | Restore the expected context on one file. | SELinux label change |
| `restorecon -v /var/www/html/index.html` | Relabel verbosely. | SELinux label change |
| `restorecon -n -v /var/www/html/index.html` | Preview a relabel. | Read-only |
| `sudo restorecon -Rv /srv/web` | Relabel a directory recursively. | Recursive SELinux label change |
| `sudo restorecon -RFv /srv/web` | Force recursive relabeling while following the command's documented semantics. | Broad SELinux label change |
| `sudo restorecon -e /srv/web/cache -Rv /srv/web` | Exclude a path during recursive relabeling. | Recursive SELinux label change |
| `matchpathcon -V /srv/web/index.html` | Compare the current label with policy. | Read-only |
| `ls -Zd /srv/web /srv/web/index.html` | Display current labels. | Read-only |
| `sudo semanage fcontext -a -t httpd_sys_content_t '/srv/web(/.*)?'` | Create a persistent path rule. | SELinux policy configuration change |
| `sudo restorecon -Rv /srv/web` | Apply the newly defined rule. | Recursive SELinux label change |
| `ausearch -m AVC -ts recent -i` | Review recent SELinux denials. | Read-only |
| `fixfiles -n onboot` | Request or inspect full relabel behavior only according to platform procedure. | System-wide relabel planning |

## 6. Detailed command examples

### 6.1 Restore the expected context on one file.

**Risk level:** SELinux label change

```bash
restorecon /var/www/html/index.html
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Relabel verbosely.

**Risk level:** SELinux label change

```bash
restorecon -v /var/www/html/index.html
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Preview a relabel.

**Risk level:** Read-only

```bash
restorecon -n -v /var/www/html/index.html
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Relabel a directory recursively.

**Risk level:** Recursive SELinux label change

```bash
sudo restorecon -Rv /srv/web
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Force recursive relabeling while following the command's documented semantics.

**Risk level:** Broad SELinux label change

```bash
sudo restorecon -RFv /srv/web
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Exclude a path during recursive relabeling.

**Risk level:** Recursive SELinux label change

```bash
sudo restorecon -e /srv/web/cache -Rv /srv/web
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Compare the current label with policy.

**Risk level:** Read-only

```bash
matchpathcon -V /srv/web/index.html
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Display current labels.

**Risk level:** Read-only

```bash
ls -Zd /srv/web /srv/web/index.html
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Create a persistent path rule.

**Risk level:** SELinux policy configuration change

```bash
sudo semanage fcontext -a -t httpd_sys_content_t '/srv/web(/.*)?'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Apply the newly defined rule.

**Risk level:** Recursive SELinux label change

```bash
sudo restorecon -Rv /srv/web
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Review recent SELinux denials.

**Risk level:** Read-only

```bash
ausearch -m AVC -ts recent -i
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Request or inspect full relabel behavior only according to platform procedure.

**Risk level:** System-wide relabel planning

```bash
fixfiles -n onboot
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- Verbose output normally shows paths whose contexts changed.
- No output without `-v` often means success with no displayed detail.
- A preview that repeatedly proposes changes after relabeling can indicate policy, overlay, immutable, xattr, or filesystem issues.
- The expected type should be confirmed with `matchpathcon`.
- An AVC after correct relabeling may require boolean, application, path, or policy analysis rather than another relabel.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Preview large recursive relabels and exclude application data that has separate labeling requirements.
- Do not run full-system relabel commands casually.
- Create a persistent `semanage fcontext` rule before relabeling custom service paths.
- Keep SELinux enforcing during normal troubleshooting and solve the specific policy or labeling issue.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Create a custom web directory.
2. Observe its initial label.
3. Add a persistent fcontext rule.
4. Preview and apply `restorecon`.
5. Verify access under SELinux enforcing mode.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
