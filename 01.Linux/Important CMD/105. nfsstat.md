# 105. nfsstat

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`nfsstat` displays NFS client and server RPC operation counters. It helps identify call volume, retransmissions, protocol-version usage, operation mix, cache behavior, and server workload.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** nfs-utils
- Check command availability:

```bash
command -v nfsstat 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Counters are normally cumulative since boot or module/service initialization.
- Client retransmissions can indicate packet loss, timeout, congestion, server delay, or an unsuitable transport path.
- NFSv3 and NFSv4 have different operation sets and state models.
- High call count is not itself a problem; compare rates, latency, retransmissions, and application impact.
- NFS caching means application reads and writes do not map one-to-one to network RPCs.
- Use interval tools, mount options, packet capture, and server storage metrics together.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `nfsstat` | Display combined client and server statistics. | Read-only |
| `nfsstat -c` | Display client statistics. | Read-only |
| `nfsstat -s` | Display server statistics. | Read-only |
| `nfsstat -n` | Display NFS operation counters. | Read-only |
| `nfsstat -r` | Display RPC-layer counters. | Read-only |
| `nfsstat -m` | Display mounted NFS filesystems and effective options. | Read-only |
| `nfsstat -l` | Display information in list format where supported. | Read-only |
| `nfsstat -o all` | Display all supported statistic groups. | Read-only |
| `nfsstat -Z 1` | Display interval changes and zero counters where supported. | Read-only |
| `watch -n 2 nfsstat -c` | Observe client counters repeatedly. | Read-only |
| `cat /proc/net/rpc/nfs` | Inspect raw client RPC counters. | Read-only |
| `cat /proc/net/rpc/nfsd` | Inspect raw server RPC counters. | Read-only |

## 6. Detailed command examples

### 6.1 Display combined client and server statistics.

**Risk level:** Read-only

```bash
nfsstat
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Display client statistics.

**Risk level:** Read-only

```bash
nfsstat -c
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Display server statistics.

**Risk level:** Read-only

```bash
nfsstat -s
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Display NFS operation counters.

**Risk level:** Read-only

```bash
nfsstat -n
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Display RPC-layer counters.

**Risk level:** Read-only

```bash
nfsstat -r
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Display mounted NFS filesystems and effective options.

**Risk level:** Read-only

```bash
nfsstat -m
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Display information in list format where supported.

**Risk level:** Read-only

```bash
nfsstat -l
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Display all supported statistic groups.

**Risk level:** Read-only

```bash
nfsstat -o all
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Display interval changes and zero counters where supported.

**Risk level:** Read-only

```bash
nfsstat -Z 1
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Observe client counters repeatedly.

**Risk level:** Read-only

```bash
watch -n 2 nfsstat -c
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Inspect raw client RPC counters.

**Risk level:** Read-only

```bash
cat /proc/net/rpc/nfs
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Inspect raw server RPC counters.

**Risk level:** Read-only

```bash
cat /proc/net/rpc/nfsd
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- `calls` is the total number of RPC operations.
- `retrans` counts client retransmissions and should be evaluated as a rate and percentage.
- Operation rows such as READ, WRITE, GETATTR, LOOKUP, and COMMIT show workload mix.
- `badcalls` or authentication errors require transport and server-log correlation.
- Mount output helps confirm NFS version, transport, timeout, retry, read/write size, and cache options.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Do not reset counters before preserving the incident baseline.
- A retransmission can be legitimate during failover; sustained growth under normal operation needs investigation.
- Avoid changing `timeo`, `retrans`, `soft`, or caching options without understanding data-integrity behavior.
- Use `soft` mounts only with explicit application and vendor approval.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Mount an NFS export in a lab.
2. Record client counters.
3. Read and write a test file.
4. Calculate operation deltas.
5. Correlate with `mountstats`, `ss`, and a filtered packet capture.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
