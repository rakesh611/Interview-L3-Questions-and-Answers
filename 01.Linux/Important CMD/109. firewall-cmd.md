# 109. firewall-cmd

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`firewall-cmd` is the command-line client for firewalld. It manages zones, services, ports, interfaces, sources, rich rules, policies, forwarding, masquerading, and runtime versus permanent configuration.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** firewalld
- Check command availability:

```bash
command -v firewall-cmd 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Runtime configuration is active now; permanent configuration is stored for future reload or boot.
- Zones group trust levels and rules and can be bound to interfaces or source networks.
- Services are named XML definitions that can include multiple ports and helpers.
- Rich rules support scoped source, destination, logging, rate limiting, and actions.
- Policies handle traffic between zones in newer firewalld versions.
- Direct rules are legacy/advanced and should not be the first choice.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `sudo firewall-cmd --state` | Check whether firewalld is running. | Read-only |
| `sudo firewall-cmd --get-active-zones` | Show active zone bindings. | Read-only |
| `sudo firewall-cmd --list-all` | Show the default or selected active zone. | Read-only |
| `sudo firewall-cmd --zone=public --list-all` | Show one zone. | Read-only |
| `sudo firewall-cmd --get-services` | List known service definitions. | Read-only |
| `sudo firewall-cmd --zone=public --add-service=https` | Allow HTTPS at runtime. | Runtime firewall change |
| `sudo firewall-cmd --permanent --zone=public --add-service=https` | Allow HTTPS permanently. | Persistent firewall change |
| `sudo firewall-cmd --zone=public --add-port=8443/tcp` | Open a runtime port. | Runtime firewall change |
| `sudo firewall-cmd --zone=public --add-rich-rule='rule family=ipv4 source address=192.0.2.0/24 service name=ssh accept'` | Add a source-scoped rich rule. | Runtime firewall change |
| `sudo firewall-cmd --runtime-to-permanent` | Copy all runtime configuration to permanent storage. | Broad persistent change |
| `sudo firewall-cmd --reload` | Reload permanent configuration into runtime. | Firewall reload |
| `sudo firewall-cmd --check-config` | Validate permanent configuration. | Read-only |
| `sudo firewall-cmd --zone=public --query-service=https` | Check whether a service is enabled. | Read-only |

## 6. Detailed command examples

### 6.1 Check whether firewalld is running.

**Risk level:** Read-only

```bash
sudo firewall-cmd --state
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Show active zone bindings.

**Risk level:** Read-only

```bash
sudo firewall-cmd --get-active-zones
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Show the default or selected active zone.

**Risk level:** Read-only

```bash
sudo firewall-cmd --list-all
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Show one zone.

**Risk level:** Read-only

```bash
sudo firewall-cmd --zone=public --list-all
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 List known service definitions.

**Risk level:** Read-only

```bash
sudo firewall-cmd --get-services
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Allow HTTPS at runtime.

**Risk level:** Runtime firewall change

```bash
sudo firewall-cmd --zone=public --add-service=https
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Allow HTTPS permanently.

**Risk level:** Persistent firewall change

```bash
sudo firewall-cmd --permanent --zone=public --add-service=https
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Open a runtime port.

**Risk level:** Runtime firewall change

```bash
sudo firewall-cmd --zone=public --add-port=8443/tcp
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Add a source-scoped rich rule.

**Risk level:** Runtime firewall change

```bash
sudo firewall-cmd --zone=public --add-rich-rule='rule family=ipv4 source address=192.0.2.0/24 service name=ssh accept'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Copy all runtime configuration to permanent storage.

**Risk level:** Broad persistent change

```bash
sudo firewall-cmd --runtime-to-permanent
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Reload permanent configuration into runtime.

**Risk level:** Firewall reload

```bash
sudo firewall-cmd --reload
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Validate permanent configuration.

**Risk level:** Read-only

```bash
sudo firewall-cmd --check-config
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.13 Check whether a service is enabled.

**Risk level:** Read-only

```bash
sudo firewall-cmd --zone=public --query-service=https
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- An interface normally belongs to one active zone.
- Source-based zone bindings can take precedence over interface assumptions.
- A rule present only in permanent configuration is not active until reload.
- A runtime rule disappears after reload or restart unless made permanent.
- Packet counters and deeper rule evaluation may require `nft list ruleset`.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Remote firewall changes can immediately terminate access.
- Use a firewalld timeout option, checkpoint process, or console recovery path for risky changes.
- Do not use `--runtime-to-permanent` without reviewing unrelated temporary rules.
- Opening a port does not start the service or validate application authorization.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Inspect active zones.
2. Add a temporary port with a timeout where supported.
3. Verify access and inspect the ruleset.
4. Remove the test rule.
5. Create a service definition only in an isolated lab.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
