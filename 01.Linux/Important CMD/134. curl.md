# 134. curl

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`curl` transfers data using HTTP, HTTPS, FTP, SFTP and other supported protocols. Administrators use it for API tests, TLS diagnosis, proxy validation, downloads, uploads, authentication, timing, and health checks.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** curl
- Check availability and installed version before relying on an option:

```bash
command -v curl 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- HTTP status, TCP success, TLS success, and application correctness are separate layers.
- `-f` makes selected HTTP error status codes produce failure exit status.
- `-sS` suppresses progress while preserving error messages.
- `--resolve` overrides DNS for one request while preserving the URL host and TLS SNI.
- Certificate verification should remain enabled; use the correct CA rather than `-k`.
- Shell quoting is essential for URLs, headers, JSON, and credentials.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `curl -fSs https://example.com/health` | Perform a quiet health check with useful failure status. | Read-only |
| `curl -I https://example.com/` | Fetch response headers with a HEAD request. | Read-only |
| `curl -v https://example.com/` | Show connection, TLS, and protocol diagnostics. | Read-only |
| `curl --trace-time --trace-ascii /tmp/curl.trace https://example.com/` | Write a timestamped protocol trace. | Read-only |
| `curl -o file.iso https://downloads.example.com/file.iso` | Download to an explicit file. | Read-only |
| `curl -C - -O https://downloads.example.com/file.iso` | Resume a download. | Read-only |
| `curl --retry 5 --retry-all-errors --connect-timeout 5 --max-time 60 URL` | Use bounded retries and timeouts. | Read-only |
| `curl --resolve api.example.com:443:192.0.2.20 https://api.example.com/health` | Test one IP while preserving hostname and SNI. | Read-only |
| `curl --cacert corp-ca.pem https://api.example.com/` | Use an explicit CA bundle. | Read-only |
| `curl --cert client.pem --key client.key https://api.example.com/` | Use a client certificate and key. | Read-only |
| `curl -H 'Content-Type: application/json' --data-binary @request.json https://api.example.com/v1/items` | POST a JSON file. | Read-only |
| `curl --unix-socket /run/app.sock http://localhost/health` | Query an HTTP service over a Unix socket. | Read-only |
| `curl -w 'code=%{http_code} connect=%{time_connect} tls=%{time_appconnect} start=%{time_starttransfer} total=%{time_total}\n' -o /dev/null -sS URL` | Measure request phases. | Read-only |

## 6. Detailed examples

### 6.1 Perform a quiet health check with useful failure status.

**Operational classification:** Read-only

```bash
curl -fSs https://example.com/health
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Fetch response headers with a HEAD request.

**Operational classification:** Read-only

```bash
curl -I https://example.com/
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Show connection, TLS, and protocol diagnostics.

**Operational classification:** Read-only

```bash
curl -v https://example.com/
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Write a timestamped protocol trace.

**Operational classification:** Read-only

```bash
curl --trace-time --trace-ascii /tmp/curl.trace https://example.com/
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Download to an explicit file.

**Operational classification:** Read-only

```bash
curl -o file.iso https://downloads.example.com/file.iso
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Resume a download.

**Operational classification:** Read-only

```bash
curl -C - -O https://downloads.example.com/file.iso
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Use bounded retries and timeouts.

**Operational classification:** Read-only

```bash
curl --retry 5 --retry-all-errors --connect-timeout 5 --max-time 60 URL
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Test one IP while preserving hostname and SNI.

**Operational classification:** Read-only

```bash
curl --resolve api.example.com:443:192.0.2.20 https://api.example.com/health
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Use an explicit CA bundle.

**Operational classification:** Read-only

```bash
curl --cacert corp-ca.pem https://api.example.com/
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Use a client certificate and key.

**Operational classification:** Read-only

```bash
curl --cert client.pem --key client.key https://api.example.com/
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 POST a JSON file.

**Operational classification:** Read-only

```bash
curl -H 'Content-Type: application/json' --data-binary @request.json https://api.example.com/v1/items
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.12 Query an HTTP service over a Unix socket.

**Operational classification:** Read-only

```bash
curl --unix-socket /run/app.sock http://localhost/health
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.13 Measure request phases.

**Operational classification:** Read-only

```bash
curl -w 'code=%{http_code} connect=%{time_connect} tls=%{time_appconnect} start=%{time_starttransfer} total=%{time_total}\n' -o /dev/null -sS URL
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- Verbose output lines prefixed with `*` describe curl, `>` request headers, and `<` response headers.
- HTTP 200 does not guarantee the response body is semantically correct.
- A large `time_connect` suggests transport establishment delay; large `time_appconnect` can indicate TLS delay.
- Large `time_starttransfer` after fast connection can indicate server processing or upstream delay.
- curl exit codes distinguish DNS, connection, TLS, timeout, write, and HTTP-failure classes.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Do not place passwords, bearer tokens, or private keys in shell history.
- Avoid `-k/--insecure` except for a tightly controlled diagnosis.
- Use `--fail-with-body` carefully because response bodies may contain sensitive details.
- Bound retries to avoid amplifying an outage.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Test a healthy endpoint.
2. Override DNS with `--resolve`.
3. Measure timing fields.
4. Test a wrong CA and then the correct CA.
5. Capture and interpret curl's exit status.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
