# 148. BorgBackup (borg)

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

BorgBackup is an encrypted, authenticated, deduplicating backup system. Borg 1 commonly uses commands such as `borg init` and `borg create`; Borg 2 has command changes, so always check the installed major version.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** borgbackup
- Check availability and installed version before relying on an option:

```bash
command -v BorgBackup 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- Repositories contain deduplicated chunks referenced by archives.
- Encryption modes determine where keys are stored and how repository compromise is handled.
- Compression occurs before encrypted repository storage.
- Prune removes archive references; compact reclaims segment space in common Borg 1 workflows.
- Repository and key backups are separate requirements.
- Application-consistent backup still requires quiescing or supported dumps.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `borg --version` | Display the installed Borg version. | Read-only |
| `export BORG_REPO=/backup/borgrepo` | Set a repository environment variable. | Read-only |
| `export BORG_PASSCOMMAND='cat /root/.config/borg/passphrase'` | Read a passphrase from a protected command source. | Read-only |
| `borg init --encryption=repokey-blake2 "$BORG_REPO"` | Initialize a Borg 1 repository. | Backup repository creation |
| `borg create --stats --progress '::{hostname}-{now:%Y-%m-%dT%H:%M:%S}' /etc /home` | Create an archive. | Backup creation |
| `borg list` | List archives. | Read-only |
| `borg list '::ARCHIVE'` | List archive contents. | Read-only |
| `borg info '::ARCHIVE'` | Display archive information. | Read-only |
| `borg extract '::ARCHIVE' etc/hosts` | Restore a selected path into the current directory. | File restoration |
| `borg check --verify-data` | Verify repository and stored data. | Repository verification |
| `borg prune --list --keep-daily=7 --keep-weekly=5 --keep-monthly=12` | Apply retention rules. | Destructive archive removal |
| `borg compact` | Reclaim repository space after prune in Borg 1. | Repository maintenance |
| `borg mount '::ARCHIVE' /mnt/borg` | Mount an archive through FUSE. | Temporary backup mount |
| `borg key export "$BORG_REPO" /root/borg-key-backup` | Export repository key material. | Sensitive key backup |
| `borg break-lock "$BORG_REPO"` | Break stale locks only after proving no Borg process is active. | Repository lock change |

## 6. Detailed examples

### 6.1 Display the installed Borg version.

**Operational classification:** Read-only

```bash
borg --version
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Set a repository environment variable.

**Operational classification:** Read-only

```bash
export BORG_REPO=/backup/borgrepo
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Read a passphrase from a protected command source.

**Operational classification:** Read-only

```bash
export BORG_PASSCOMMAND='cat /root/.config/borg/passphrase'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Initialize a Borg 1 repository.

**Operational classification:** Backup repository creation

```bash
borg init --encryption=repokey-blake2 "$BORG_REPO"
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Create an archive.

**Operational classification:** Backup creation

```bash
borg create --stats --progress '::{hostname}-{now:%Y-%m-%dT%H:%M:%S}' /etc /home
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 List archives.

**Operational classification:** Read-only

```bash
borg list
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 List archive contents.

**Operational classification:** Read-only

```bash
borg list '::ARCHIVE'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Display archive information.

**Operational classification:** Read-only

```bash
borg info '::ARCHIVE'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Restore a selected path into the current directory.

**Operational classification:** File restoration

```bash
borg extract '::ARCHIVE' etc/hosts
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Verify repository and stored data.

**Operational classification:** Repository verification

```bash
borg check --verify-data
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Apply retention rules.

**Operational classification:** Destructive archive removal

```bash
borg prune --list --keep-daily=7 --keep-weekly=5 --keep-monthly=12
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.12 Reclaim repository space after prune in Borg 1.

**Operational classification:** Repository maintenance

```bash
borg compact
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.13 Mount an archive through FUSE.

**Operational classification:** Temporary backup mount

```bash
borg mount '::ARCHIVE' /mnt/borg
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.14 Export repository key material.

**Operational classification:** Sensitive key backup

```bash
borg key export "$BORG_REPO" /root/borg-key-backup
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.15 Break stale locks only after proving no Borg process is active.

**Operational classification:** Repository lock change

```bash
borg break-lock "$BORG_REPO"
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- Archive statistics distinguish original, compressed, and deduplicated sizes.
- A small deduplicated size means most chunks already existed.
- Repository check success should be supplemented by actual restore testing.
- A lock error usually indicates an active or stale operation and must be investigated.
- Borg 2 syntax can differ significantly from Borg 1 examples.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Back up exported keys separately from the repository and protect passphrases.
- Do not run `break-lock` during an active backup.
- Review prune selections before accepting removal.
- Use off-site and immutable protections against credential-based deletion.
- Test the commands against the installed Borg major version.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Initialize an encrypted local repository.
2. Create two archives.
3. Compare deduplication statistics.
4. Restore one file.
5. Run check and export the key to protected media.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
