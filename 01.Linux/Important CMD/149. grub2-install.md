# 149. grub2-install

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`grub2-install` installs GRUB bootloader code and modules for a selected firmware platform and target. Its correct use differs sharply between BIOS and UEFI systems and among distributions; an incorrect command can make the system unbootable.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** grub2-tools and platform-specific GRUB EFI/BIOS packages
- Check availability and installed version before relying on an option:

```bash
command -v grub2-install 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- BIOS installation usually writes boot code to a whole disk, not a partition.
- UEFI installation uses an EFI System Partition and firmware boot entry, but vendor distributions may require package-based recovery rather than direct `grub2-install`.
- Secure Boot depends on signed shim and GRUB components and can be broken by unsupported replacement.
- `grub2-install` installs bootloader components; `grub2-mkconfig` generates menu configuration.
- The root, `/boot`, and EFI System Partition layout must be known before repair.
- RAID, LVM, encryption, multipath, and mirrored boot disks require platform-specific planning.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `grub2-install --version` | Display the installed GRUB version. | Read-only |
| `test -d /sys/firmware/efi && echo UEFI \|\| echo BIOS` | Identify the current firmware boot mode. | Read-only |
| `findmnt / /boot /boot/efi` | Inspect root, boot, and EFI mounts. | Read-only |
| `lsblk -o NAME,PATH,TYPE,SIZE,FSTYPE,PARTTYPE,PARTLABEL,MOUNTPOINTS,MODEL,SERIAL` | Inventory the boot storage layout. | Read-only |
| `efibootmgr -v` | Inspect UEFI boot entries. | Read-only |
| `grubby --info=ALL` | Inspect configured kernels on supported enterprise distributions. | Read-only |
| `grub2-probe --target=fs /boot` | Probe the filesystem used by `/boot`. | Read-only |
| `grub2-probe --target=device /boot` | Probe the underlying boot device. | Read-only |
| `sudo grub2-install --recheck /dev/sda` | Example BIOS installation to a whole disk only after platform validation. | Critical bootloader write |
| `sudo grub2-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=Linux` | Generic UEFI example; may be inappropriate on vendor Secure Boot systems. | Critical UEFI bootloader write |
| `sudo grub2-mkconfig -o /boot/grub2/grub.cfg` | Generate GRUB configuration at the distribution-approved path. | Boot configuration rewrite |
| `sudo cp -a /boot/grub2 /root/grub2-backup && sudo cp -a /boot/efi/EFI /root/EFI-backup` | Back up bootloader files before repair. | Backup creation |
| `journalctl -b -1 -k \| grep -iE 'efi\|grub\|boot\|secure'` | Review previous-boot evidence. | Read-only |
| `rpm -qa \| grep -E '^grub2\|^shim' \| sort` | Inventory GRUB and shim packages on RPM systems. | Read-only |

## 6. Detailed examples

### 6.1 Display the installed GRUB version.

**Operational classification:** Read-only

```bash
grub2-install --version
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Identify the current firmware boot mode.

**Operational classification:** Read-only

```bash
test -d /sys/firmware/efi && echo UEFI || echo BIOS
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Inspect root, boot, and EFI mounts.

**Operational classification:** Read-only

```bash
findmnt / /boot /boot/efi
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Inventory the boot storage layout.

**Operational classification:** Read-only

```bash
lsblk -o NAME,PATH,TYPE,SIZE,FSTYPE,PARTTYPE,PARTLABEL,MOUNTPOINTS,MODEL,SERIAL
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Inspect UEFI boot entries.

**Operational classification:** Read-only

```bash
efibootmgr -v
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Inspect configured kernels on supported enterprise distributions.

**Operational classification:** Read-only

```bash
grubby --info=ALL
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Probe the filesystem used by `/boot`.

**Operational classification:** Read-only

```bash
grub2-probe --target=fs /boot
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Probe the underlying boot device.

**Operational classification:** Read-only

```bash
grub2-probe --target=device /boot
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Example BIOS installation to a whole disk only after platform validation.

**Operational classification:** Critical bootloader write

```bash
sudo grub2-install --recheck /dev/sda
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Generic UEFI example; may be inappropriate on vendor Secure Boot systems.

**Operational classification:** Critical UEFI bootloader write

```bash
sudo grub2-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=Linux
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Generate GRUB configuration at the distribution-approved path.

**Operational classification:** Boot configuration rewrite

```bash
sudo grub2-mkconfig -o /boot/grub2/grub.cfg
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.12 Back up bootloader files before repair.

**Operational classification:** Backup creation

```bash
sudo cp -a /boot/grub2 /root/grub2-backup && sudo cp -a /boot/efi/EFI /root/EFI-backup
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.13 Review previous-boot evidence.

**Operational classification:** Read-only

```bash
journalctl -b -1 -k | grep -iE 'efi|grub|boot|secure'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.14 Inventory GRUB and shim packages on RPM systems.

**Operational classification:** Read-only

```bash
rpm -qa | grep -E '^grub2|^shim' | sort
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- Firmware mode must match the target architecture passed to GRUB.
- A successful installation command does not prove firmware will select the new entry.
- Missing modules or embedding errors often indicate an unsuitable target layout.
- Secure Boot failures can occur after replacing signed vendor components.
- Menu-generation paths and EFI stub configuration differ by distribution release.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Do not run `grub2-install` on a working production host without a documented recovery procedure and console access.
- Use distribution-specific bootloader recovery documentation, especially for UEFI Secure Boot.
- Back up the partition table, EFI System Partition, `/boot`, GRUB configuration, and current boot entries.
- Never guess the disk from `/dev/sdX`; verify serial, WWID, firmware mode, and actual boot path.
- Maintain bootable rescue media and test the result with a controlled reboot.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Build separate BIOS and UEFI virtual machines.
2. Inventory each boot layout.
3. Back up boot files and firmware entries.
4. Repair GRUB using the distribution procedure.
5. Reboot and validate every installed kernel entry.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
