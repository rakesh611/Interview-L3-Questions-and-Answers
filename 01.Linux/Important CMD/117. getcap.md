# 117. getcap

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`getcap` displays file capabilities stored in the `security.capability` extended attribute. File capabilities can grant selected privileged kernel operations to an executable without making it fully setuid root.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** libcap
- Check command availability:

```bash
command -v getcap 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Linux capabilities divide traditional root privilege into individual capability bits.
- File capability sets include permitted, inheritable, and effective behavior.
- Capabilities are applied during `execve` and interact with process bounding, ambient, permitted, effective, and inheritable sets.
- A capability can be powerful enough to compromise the host even when it sounds narrow.
- Copying files, packaging, backup tools, and filesystems can lose capability xattrs.
- Scripts do not receive file capabilities in the same straightforward way as ELF executables.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `getcap /usr/bin/ping` | Display capabilities on one executable. | Read-only |
| `getcap -v /usr/bin/ping` | Display the file name even when no capability is present. | Read-only |
| `sudo getcap -r /usr /opt 2>/dev/null` | Recursively inventory file capabilities. | Read-only |
| `getcap -n /usr/bin/ping` | Display root UID information for namespaced file capabilities where supported. | Read-only |
| `getfattr -n security.capability -e hex /usr/bin/ping` | Inspect the raw capability xattr. | Read-only |
| `capsh --decode=0x0000000000002000` | Decode a numeric capability mask. | Read-only |
| `pscap` | List process capabilities when the utility is installed. | Read-only |
| `grep '^Cap' /proc/1234/status` | Inspect a process's capability masks. | Read-only |
| `rpm -Vf /usr/bin/ping` | Verify packaged metadata on an RPM system. | Read-only |
| `findmnt -T /usr/bin/ping -o TARGET,SOURCE,FSTYPE,OPTIONS` | Check the containing filesystem and mount options. | Read-only |

## 6. Detailed command examples

### 6.1 Display capabilities on one executable.

**Risk level:** Read-only

```bash
getcap /usr/bin/ping
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Display the file name even when no capability is present.

**Risk level:** Read-only

```bash
getcap -v /usr/bin/ping
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Recursively inventory file capabilities.

**Risk level:** Read-only

```bash
sudo getcap -r /usr /opt 2>/dev/null
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Display root UID information for namespaced file capabilities where supported.

**Risk level:** Read-only

```bash
getcap -n /usr/bin/ping
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Inspect the raw capability xattr.

**Risk level:** Read-only

```bash
getfattr -n security.capability -e hex /usr/bin/ping
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Decode a numeric capability mask.

**Risk level:** Read-only

```bash
capsh --decode=0x0000000000002000
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 List process capabilities when the utility is installed.

**Risk level:** Read-only

```bash
pscap
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Inspect a process's capability masks.

**Risk level:** Read-only

```bash
grep '^Cap' /proc/1234/status
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Verify packaged metadata on an RPM system.

**Risk level:** Read-only

```bash
rpm -Vf /usr/bin/ping
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Check the containing filesystem and mount options.

**Risk level:** Read-only

```bash
findmnt -T /usr/bin/ping -o TARGET,SOURCE,FSTYPE,OPTIONS
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- An output suffix such as `cap_net_raw=ep` indicates capability sets assigned to the file.
- `e` commonly means effective and `p` permitted in textual file-capability notation.
- No output normally means no file capability is set.
- A file capability can fail to take effect when blocked by `no_new_privs`, bounding sets, container policy, or filesystem behavior.
- Unexpected capabilities on writable custom binaries are a high-priority security finding.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Audit recursive findings carefully; several capabilities can provide root-equivalent paths.
- Do not replace setuid with capabilities without threat modelling and testing.
- Preserve xattrs during backup and deployment.
- Validate package ownership and integrity for every unexpected privileged binary.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Copy a safe test binary into a lab directory.
2. Inspect its initial capabilities.
3. Assign a low-scope capability in an isolated environment.
4. Run it under a non-root account and inspect process masks.
5. Remove the capability.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
