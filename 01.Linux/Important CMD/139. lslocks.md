# 139. lslocks

> **Scope:** Corporate/L3 Linux administration, incident troubleshooting, safe change execution, and interview preparation.

## 1. What it is

`lslocks` formats the kernel's file-lock table into human-readable columns such as command, PID, type, mode, blocked state, byte range, and pathname.

## 2. Production importance

- Provides a repeatable method for diagnosing or controlling the relevant Linux subsystem.
- Produces evidence suitable for incident timelines, change records, problem management, and audit review.
- Encourages before-and-after measurement instead of unverified tuning or repair.
- Separates command success from actual application availability, security, and data integrity.

## 3. Package or kernel interface

- **Primary package/interface:** util-linux
- Check availability and installed version before relying on an option:

```bash
command -v lslocks 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
COMMAND --version 2>/dev/null || COMMAND --help
```

## 4. Core concepts

- The command reads kernel lock information, primarily from `/proc/locks`.
- Pathname resolution may be incomplete or unavailable.
- Open-file-description locks do not always map to a traditional process owner in the same way as POSIX locks.
- Locks are dynamic snapshots.
- A displayed lock is not automatically a fault.
- Blocked waiters and owners should be analyzed as an application dependency chain.

## 5. Important commands

| Command | Purpose | Risk |
|---|---|---|
| `lslocks` | Display current file locks. | Read-only |
| `lslocks -p 1234` | Filter by PID. | Read-only |
| `lslocks -o COMMAND,PID,TYPE,MODE,BLOCKER,START,END,PATH` | Choose explicit columns. | Read-only |
| `lslocks --output-all` | Display all available columns where supported. | Read-only |
| `lslocks -J` | Produce JSON output. | Read-only |
| `lslocks --noheadings --raw` | Produce script-oriented output. | Read-only |
| `watch -n 1 'lslocks -o PID,COMMAND,TYPE,MODE,BLOCKER,PATH'` | Observe lock changes. | Read-only |
| `cat /proc/locks` | Compare with the raw kernel interface. | Read-only |
| `ps -p 1234 -o pid,ppid,user,stat,wchan:32,etime,cmd` | Inspect a holder or waiter. | Read-only |
| `lsof -p 1234` | Inspect open files for a process. | Read-only |
| `strace -tt -T -e trace=flock,fcntl -p 1234` | Trace lock calls on a selected process. | Live tracing |

## 6. Detailed examples

### 6.1 Display current file locks.

**Operational classification:** Read-only

```bash
lslocks
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.2 Filter by PID.

**Operational classification:** Read-only

```bash
lslocks -p 1234
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.3 Choose explicit columns.

**Operational classification:** Read-only

```bash
lslocks -o COMMAND,PID,TYPE,MODE,BLOCKER,START,END,PATH
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.4 Display all available columns where supported.

**Operational classification:** Read-only

```bash
lslocks --output-all
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.5 Produce JSON output.

**Operational classification:** Read-only

```bash
lslocks -J
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.6 Produce script-oriented output.

**Operational classification:** Read-only

```bash
lslocks --noheadings --raw
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.7 Observe lock changes.

**Operational classification:** Read-only

```bash
watch -n 1 'lslocks -o PID,COMMAND,TYPE,MODE,BLOCKER,PATH'
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.8 Compare with the raw kernel interface.

**Operational classification:** Read-only

```bash
cat /proc/locks
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.9 Inspect a holder or waiter.

**Operational classification:** Read-only

```bash
ps -p 1234 -o pid,ppid,user,stat,wchan:32,etime,cmd
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.10 Inspect open files for a process.

**Operational classification:** Read-only

```bash
lsof -p 1234
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

### 6.11 Trace lock calls on a selected process.

**Operational classification:** Live tracing

```bash
strace -tt -T -e trace=flock,fcntl -p 1234
```

Execution checklist:

1. Confirm the exact target and configuration owner.
2. Record the current state and relevant service metric.
3. Run the command with the least privilege necessary.
4. Capture standard output, standard error, exit code, and timestamp.
5. Validate the result using an independent command or application test.
6. Roll back immediately if the intended metric does not improve.

## 7. Output interpretation

- `MODE` identifies read or write locking and can mark mandatory behavior where applicable.
- `BLOCKER` can identify the PID preventing a waiting lock request.
- `START` and `END` define byte-range locks.
- A blank path does not mean the lock is invalid; pathname resolution can fail.
- Repeated blocked locks with service latency can indicate application serialization or deadlock-like behavior.

## 8. Corporate/L3 troubleshooting workflow

1. **Confirm impact:** Record affected applications, users, start time, error rate, latency, and business severity.
2. **Identify scope:** Determine whether the issue is host-wide, process-specific, filesystem-specific, network-path-specific, or limited to one namespace.
3. **Preserve evidence:** Save logs, configuration, counters, command output, package versions, and timestamps before changing state.
4. **Establish ownership:** Identify whether systemd, NetworkManager, firewalld, TuneD, udev, a backup scheduler, configuration management, or the application owns the setting.
5. **Use read-only inspection first:** Confirm the hypothesis with at least two independent evidence sources.
6. **Control the test:** Make only one bounded change and define the rollback command in advance.
7. **Measure service behavior:** Validate latency, throughput, errors, data consistency, authentication, and client experience.
8. **Persist correctly:** Apply successful changes through the supported configuration source rather than an ad hoc runtime command.
9. **Close the incident:** Document root cause, contributing conditions, corrective action, monitoring, and prevention.

## 9. Production cautions

- Do not remove lock files or kill processes without understanding application locking design.
- Use JSON or explicit columns in automation because defaults can change.
- Protect process and pathname output.
- For distributed filesystems, include server-side lock evidence.
- Replace all example users, paths, devices, addresses, ports, repositories, and services only after validation.
- Keep console or out-of-band access for firewall, bootloader, storage, authentication, and network changes.
- Back up data and configuration before destructive, recursive, or boot-critical commands.

## 10. Practice lab

1. Create an exclusive lock.
2. Create a blocked waiter.
3. Identify owner and blocker.
4. Trace the lock calls.
5. Release locks and verify cleanup.

## 11. Evidence template

```text
Host and environment:
Date/time/timezone:
OS and kernel:
Package/tool version:
Affected service:
Business symptom:
Baseline command and output:
Hypothesis:
Change command:
Rollback command:
Validation result:
Related logs:
Root cause:
Corrective action:
Preventive action:
```

## 12. L3 interview questions

- What kernel interface, service, or data source does the tool use?
- Which commands are read-only, runtime-changing, persistent, destructive, or security-sensitive?
- What common output can be misinterpreted?
- Which adjacent tools confirm the same hypothesis?
- How would you perform the change remotely without losing access?
- What is the rollback and how would you validate it?
