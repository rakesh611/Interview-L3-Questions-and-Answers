# 96. sssctl

> **Audience:** Linux L2/L3 administrators, performance engineers, and production support teams.

## 1. Overview

Diagnoses SSSD configuration, domains, users, groups, cache, access checks, logs, and support data.

## 2. Package and availability

- **Package/interface:** `sssd-tools`
- Verify with `command -v COMMAND`, `rpm -q PACKAGE`, or the distribution equivalent.
- Use `man COMMAND` and `COMMAND --help` on the target host because options can vary by release.

## 3. Important concepts

- NSS lookup, PAM authentication, and authorization are different paths.
- Cached success can hide an offline backend.
- DNS, time, TLS, and directory connectivity commonly interact.

## 4. Important commands

| Command | Purpose |
|---|---|
| `sudo sssctl config-check` | Validate configuration |
| `sudo sssctl domain-list` | List domains |
| `sudo sssctl domain-status example.com` | Show domain state |
| `sudo sssctl user-checks user@example.com` | Test identity and access |
| `sudo sssctl user-show user@example.com` | Show cached user |
| `sudo sssctl group-show group@example.com` | Show cached group |
| `sudo sssctl cache-expire -u user@example.com` | Expire one user |
| `sudo sssctl cache-expire -E` | Expire all cache entries |
| `sudo sssctl debug-level 6` | Set temporary debug where supported |
| `sudo sssctl analyze` | Analyze logs where supported |
| `sudo sssctl create-report /tmp/sssd-report.tar.gz` | Create a report where supported |

## 5. Detailed examples

### 5.1 Validate configuration

```bash
sudo sssctl config-check
```

**Use:** Validate configuration. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.2 List domains

```bash
sudo sssctl domain-list
```

**Use:** List domains. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.3 Show domain state

```bash
sudo sssctl domain-status example.com
```

**Use:** Show domain state. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.4 Test identity and access

```bash
sudo sssctl user-checks user@example.com
```

**Use:** Test identity and access. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.5 Show cached user

```bash
sudo sssctl user-show user@example.com
```

**Use:** Show cached user. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.6 Show cached group

```bash
sudo sssctl group-show group@example.com
```

**Use:** Show cached group. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.7 Expire one user

```bash
sudo sssctl cache-expire -u user@example.com
```

**Use:** Expire one user. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.8 Expire all cache entries

```bash
sudo sssctl cache-expire -E
```

**Use:** Expire all cache entries. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.9 Set temporary debug where supported

```bash
sudo sssctl debug-level 6
```

**Use:** Set temporary debug where supported. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.10 Analyze logs where supported

```bash
sudo sssctl analyze
```

**Use:** Analyze logs where supported. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

### 5.11 Create a report where supported

```bash
sudo sssctl create-report /tmp/sssd-report.tar.gz
```

**Use:** Create a report where supported. Run it first in a lab or with read-only scope when possible.

**What to record:** timestamp, hostname, return code, relevant identifiers, and the before/after state.

## 6. Output interpretation checklist

- Confirm that the command targeted the intended PID, device, interface, filesystem, user, service, or namespace.
- Distinguish cumulative counters from interval rates and point-in-time snapshots.
- Compare the result with a known baseline and with application symptoms.
- Correlate with `journalctl`, kernel logs, service logs, and neighboring subsystem tools.
- Treat missing output as a possible permission, namespace, package, or version issue before concluding that no problem exists.

## 7. Production troubleshooting workflow

1. Record the incident time, hostname, kernel version, and affected service.
2. Run read-only inventory and status commands first.
3. For changing metrics, collect at least five interval samples.
4. Identify the narrowest affected object and avoid host-wide changes where possible.
5. Form one hypothesis and choose one measurable corrective action.
6. Capture the original value or configuration and prepare rollback.
7. Apply the change through the owning manager, such as systemd, NetworkManager, LVM, or the service configuration.
8. Repeat the same measurements and compare application latency, errors, throughput, and saturation.
9. Roll back if the intended service metric does not improve.
10. Document root cause, evidence, corrective action, and prevention.

## 8. Production cautions

- Reports can contain sensitive identity data.
- Use scoped cache expiration first.
- Validate config and permissions before restart.
- Commands that resize, repair, create, remove, flush, signal, reload, or reconfigure can cause outage or data loss.
- Replace example names and addresses only after validating ownership and dependencies.
- Use change control, verified backups, console access, and a rollback plan for storage, network, time, scheduler, and authentication changes.

## 9. Quick lab

1. Run the first three inspection commands on a non-production host.
2. Save the output and explain every major field.
3. Trigger one safe, controlled state change if the tool supports it.
4. Re-run the inspection and compare the output.
5. Restore the original state and document the rollback.

## 10. L3 interview review

- Explain what kernel or service interface the command reads or changes.
- Identify whether the output is a snapshot, cumulative counter, or interval rate.
- Name two misleading interpretations and the additional evidence needed.
- Explain persistence, configuration ownership, and rollback.
- Describe the safest escalation evidence bundle for vendor support.
