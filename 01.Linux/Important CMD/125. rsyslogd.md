# 125. rsyslogd

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`rsyslogd` is a high-performance system logging daemon. It receives messages from local sockets, the journal, files, and networks; parses and filters them; writes files or databases; and forwards them to remote collectors.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** rsyslog
- Check command availability:

```bash
command -v rsyslogd 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Inputs, rulesets, filters, templates, actions, and queues form the processing pipeline.
- Modern RainerScript configuration is more expressive than legacy selector syntax.
- Reliable remote forwarding normally uses TCP or TLS with disk-assisted queues.
- Message duplication can result from multiple inputs or forwarding loops.
- Queue sizing and disk space determine behavior during remote outages.
- Configuration validation should precede restart or reload.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `rsyslogd -v` | Display version and build information. | Read-only |
| `sudo rsyslogd -N1` | Validate configuration syntax. | Read-only |
| `sudo rsyslogd -N1 -f /etc/rsyslog.conf` | Validate a selected configuration file. | Read-only |
| `systemctl status rsyslog` | Check service status. | Read-only |
| `sudo systemctl reload rsyslog` | Reload validated configuration. | Service configuration change |
| `journalctl -u rsyslog -b` | Review current-boot daemon logs. | Read-only |
| `logger -t rsyslog-test -p local0.notice 'routing test 12345'` | Inject a controlled test event. | Read-only |
| `grep 'routing test 12345' /var/log/messages /var/log/syslog 2>/dev/null` | Confirm local file routing. | Read-only |
| `ss -lntup \| grep -E ':(514\|6514)\b'` | Check common syslog listeners. | Read-only |
| `sudo rsyslogd -dn -f /etc/rsyslog.conf` | Run foreground debug mode only in a controlled test. | High-volume debug test |
| `grep -RinE 'module\(\|input\(\|action\(\|omfwd\|imjournal\|imuxsock\|queue\.' /etc/rsyslog.conf /etc/rsyslog.d 2>/dev/null` | Review major configuration objects. | Read-only |
| `find /var/lib/rsyslog -maxdepth 2 -type f -ls` | Inspect queue or state files. | Read-only |
| `openssl s_client -connect loghost.example.com:6514 -servername loghost.example.com` | Test TLS reachability to a remote collector. | Read-only |

## 6. Detailed command examples

### 6.1 Display version and build information.

**Risk level:** Read-only

```bash
rsyslogd -v
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Validate configuration syntax.

**Risk level:** Read-only

```bash
sudo rsyslogd -N1
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Validate a selected configuration file.

**Risk level:** Read-only

```bash
sudo rsyslogd -N1 -f /etc/rsyslog.conf
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Check service status.

**Risk level:** Read-only

```bash
systemctl status rsyslog
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Reload validated configuration.

**Risk level:** Service configuration change

```bash
sudo systemctl reload rsyslog
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Review current-boot daemon logs.

**Risk level:** Read-only

```bash
journalctl -u rsyslog -b
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Inject a controlled test event.

**Risk level:** Read-only

```bash
logger -t rsyslog-test -p local0.notice 'routing test 12345'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Confirm local file routing.

**Risk level:** Read-only

```bash
grep 'routing test 12345' /var/log/messages /var/log/syslog 2>/dev/null
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Check common syslog listeners.

**Risk level:** Read-only

```bash
ss -lntup | grep -E ':(514|6514)\b'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Run foreground debug mode only in a controlled test.

**Risk level:** High-volume debug test

```bash
sudo rsyslogd -dn -f /etc/rsyslog.conf
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Review major configuration objects.

**Risk level:** Read-only

```bash
grep -RinE 'module\(|input\(|action\(|omfwd|imjournal|imuxsock|queue\.' /etc/rsyslog.conf /etc/rsyslog.d 2>/dev/null
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Inspect queue or state files.

**Risk level:** Read-only

```bash
find /var/lib/rsyslog -maxdepth 2 -type f -ls
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.13 Test TLS reachability to a remote collector.

**Risk level:** Read-only

```bash
openssl s_client -connect loghost.example.com:6514 -servername loghost.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- Validation errors include file, line, and parser details that should be fixed before reload.
- A growing disk-assisted queue indicates forwarding failure or receiver backpressure.
- Local message success with remote absence narrows the issue to ruleset, queue, network, TLS, or receiver processing.
- Duplicate records require checking journald forwarding, imjournal state, imuxsock, application direct logging, and forwarding loops.
- Action suspension messages show retries and remote-destination failures.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Debug mode can produce enormous output and should not replace the production daemon on a busy host.
- Protect TLS private keys and CA configuration.
- Size queue disks and retention to survive expected collector outages.
- Avoid unauthenticated plain-text forwarding across untrusted networks.
- Test filters with a unique tag before applying them to broad production traffic.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Validate the current configuration.
2. Create a test rule routing a unique tag to a separate file.
3. Send test messages with `logger`.
4. Configure a lab TCP/TLS forward action with a queue.
5. Stop the receiver, inspect queueing, restart it, and confirm drain.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
