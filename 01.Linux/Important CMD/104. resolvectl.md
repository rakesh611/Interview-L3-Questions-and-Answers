# 104. resolvectl

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`resolvectl` queries and controls `systemd-resolved`. It shows global and per-link DNS servers, search domains, route-only domains, DNSSEC state, cache statistics, and lookup results.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** systemd-resolved
- Check command availability:

```bash
command -v resolvectl 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- Per-link DNS permits split-DNS designs for VPNs, management networks, and multiple uplinks.
- A route-only domain begins with `~` and directs matching queries without appending a search suffix.
- `/etc/resolv.conf` may be a symlink to a systemd-resolved stub or generated resolver file.
- NetworkManager can provide per-link DNS configuration to systemd-resolved.
- A direct query to a DNS server with `dig @server` tests a different path from the local stub resolver.
- Not every enterprise distribution enables systemd-resolved by default.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `resolvectl status` | Display global and per-link resolver state. | Read-only |
| `resolvectl query www.example.com` | Resolve a name through systemd-resolved. | Read-only |
| `resolvectl query 192.0.2.10` | Perform reverse lookup through systemd-resolved. | Read-only |
| `resolvectl dns` | List DNS servers by interface. | Read-only |
| `resolvectl domain` | List search and routing domains. | Read-only |
| `resolvectl statistics` | Display cache and transaction statistics. | Read-only |
| `sudo resolvectl flush-caches` | Flush resolver caches. | Operational change |
| `sudo resolvectl reset-statistics` | Reset resolver statistics. | Operational change |
| `sudo resolvectl dns eth0 192.0.2.53 192.0.2.54` | Set runtime DNS servers for `eth0`. | Runtime network change |
| `sudo resolvectl domain eth0 example.com '~corp.example'` | Set a search domain and route-only domain. | Runtime network change |
| `resolvectl default-route` | Show per-link DNS default-route state. | Read-only |
| `readlink -f /etc/resolv.conf; cat /etc/resolv.conf` | Identify the active resolver-file model. | Read-only |

## 6. Detailed command examples

### 6.1 Display global and per-link resolver state.

**Risk level:** Read-only

```bash
resolvectl status
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Resolve a name through systemd-resolved.

**Risk level:** Read-only

```bash
resolvectl query www.example.com
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Perform reverse lookup through systemd-resolved.

**Risk level:** Read-only

```bash
resolvectl query 192.0.2.10
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 List DNS servers by interface.

**Risk level:** Read-only

```bash
resolvectl dns
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 List search and routing domains.

**Risk level:** Read-only

```bash
resolvectl domain
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Display cache and transaction statistics.

**Risk level:** Read-only

```bash
resolvectl statistics
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Flush resolver caches.

**Risk level:** Operational change

```bash
sudo resolvectl flush-caches
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Reset resolver statistics.

**Risk level:** Operational change

```bash
sudo resolvectl reset-statistics
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Set runtime DNS servers for `eth0`.

**Risk level:** Runtime network change

```bash
sudo resolvectl dns eth0 192.0.2.53 192.0.2.54
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Set a search domain and route-only domain.

**Risk level:** Runtime network change

```bash
sudo resolvectl domain eth0 example.com '~corp.example'
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Show per-link DNS default-route state.

**Risk level:** Read-only

```bash
resolvectl default-route
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Identify the active resolver-file model.

**Risk level:** Read-only

```bash
readlink -f /etc/resolv.conf; cat /etc/resolv.conf
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- `Current DNS Server` is the server currently selected for a link.
- `DNS Servers` lists configured alternatives.
- `DNS Domain` can contain search domains and `~` route-only domains.
- `Protocols` indicates features such as DNS, LLMNR, mDNS, DNSSEC, or DNS over TLS depending on configuration.
- A lookup failure with `dig @server` success suggests local routing-domain, cache, DNSSEC, or stub configuration issues.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Runtime `resolvectl` changes may be replaced by NetworkManager or another configuration owner.
- Avoid competing owners for `/etc/resolv.conf`.
- Preserve resolver statistics and packet evidence before flushing cache during an incident.
- Validate split-DNS behavior from the actual service namespace or VPN context.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Record `resolvectl status`.
2. Query a public name and a reverse address.
3. Compare `resolvectl query` with `dig` against the selected server.
4. Inspect `/etc/resolv.conf` ownership.
5. Test a temporary per-link DNS setting only in an isolated lab.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
