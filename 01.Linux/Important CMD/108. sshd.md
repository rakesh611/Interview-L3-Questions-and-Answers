# 108. sshd

> **Level:** Linux Corporate/L3 administration and troubleshooting.

## 1. What it is

`sshd` is the OpenSSH server daemon. It listens for SSH connections, negotiates cryptography, authenticates users, applies authorization policy, and starts sessions, commands, subsystems, or forwarding channels.

## 2. Why it matters in production

- Provides a repeatable method to inspect, validate, or control an important Linux subsystem.
- Produces evidence that can be attached to incident, change, problem, audit, and capacity records.
- Helps distinguish configuration, permission, kernel, service, network, storage, security, and application failures.
- Supports before-and-after validation rather than unmeasured configuration changes.

## 3. Package and availability

- **Primary package/interface:** openssh-server
- Check command availability:

```bash
command -v sshd 2>/dev/null || true
rpm -q PACKAGE_NAME 2>/dev/null || true
```

- Read the installed manual because options vary by distribution and release:

```bash
man COMMAND
COMMAND --help
```

## 4. Core concepts

- The effective configuration can differ by user, source address, host, and `Match` blocks.
- Syntax validation should be performed before reload or restart.
- Host keys authenticate the server and must be protected and backed up appropriately.
- PAM, SELinux, NSS/SSSD, shell validity, home-directory permissions, and authorized-key permissions all affect login.
- Reloading preserves established sessions; restarting can have broader risk.
- Cryptographic algorithms and defaults vary by OpenSSH release and system crypto policy.

## 5. Important commands

| Command | Purpose | Operational risk |
|---|---|---|
| `sshd -t` | Validate configuration syntax. | Read-only |
| `sudo sshd -T` | Print effective global configuration. | Read-only |
| `sudo sshd -T -C user=appuser,host=server.example.com,addr=192.0.2.10` | Evaluate configuration for a connection context. | Read-only |
| `sudo sshd -ddd -p 2222` | Run a foreground debug instance on a test port. | Test service |
| `systemctl status sshd` | Check service state. | Read-only |
| `sudo systemctl reload sshd` | Reload configuration after validation. | Service change |
| `journalctl -u sshd -b` | Review current-boot service logs. | Read-only |
| `ss -lntp \| grep sshd` | Verify listening sockets. | Read-only |
| `sudo ssh-keygen -A` | Generate missing host keys. | Credential/key change |
| `sudo ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub` | Display a host-key fingerprint. | Read-only |
| `grep -RinE '^(Include\|Port\|ListenAddress\|PermitRootLogin\|PasswordAuthentication\|PubkeyAuthentication\|AllowUsers\|AllowGroups\|Match)' /etc/ssh/sshd_config /etc/ssh/sshd_config.d 2>/dev/null` | Review important directives. | Read-only |
| `sudo namei -om /home/appuser/.ssh/authorized_keys` | Check path ownership and permissions. | Read-only |

## 6. Detailed command examples

### 6.1 Validate configuration syntax.

**Risk level:** Read-only

```bash
sshd -t
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.2 Print effective global configuration.

**Risk level:** Read-only

```bash
sudo sshd -T
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.3 Evaluate configuration for a connection context.

**Risk level:** Read-only

```bash
sudo sshd -T -C user=appuser,host=server.example.com,addr=192.0.2.10
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.4 Run a foreground debug instance on a test port.

**Risk level:** Test service

```bash
sudo sshd -ddd -p 2222
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.5 Check service state.

**Risk level:** Read-only

```bash
systemctl status sshd
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.6 Reload configuration after validation.

**Risk level:** Service change

```bash
sudo systemctl reload sshd
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.7 Review current-boot service logs.

**Risk level:** Read-only

```bash
journalctl -u sshd -b
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.8 Verify listening sockets.

**Risk level:** Read-only

```bash
ss -lntp | grep sshd
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.9 Generate missing host keys.

**Risk level:** Credential/key change

```bash
sudo ssh-keygen -A
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.10 Display a host-key fingerprint.

**Risk level:** Read-only

```bash
sudo ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.11 Review important directives.

**Risk level:** Read-only

```bash
grep -RinE '^(Include|Port|ListenAddress|PermitRootLogin|PasswordAuthentication|PubkeyAuthentication|AllowUsers|AllowGroups|Match)' /etc/ssh/sshd_config /etc/ssh/sshd_config.d 2>/dev/null
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

### 6.12 Check path ownership and permissions.

**Risk level:** Read-only

```bash
sudo namei -om /home/appuser/.ssh/authorized_keys
```

Recommended usage:

1. Confirm the target host, user, process, path, interface, service, package, or device.
2. Record the current state before running a command that changes anything.
3. Capture standard output, standard error, exit status, and timestamp.
4. Verify the result with an independent read-only command.

## 7. How to interpret the output

- `sshd -T` expands defaults and includes, making it more reliable than reading one file.
- A login can pass key verification and still fail PAM account checks.
- Errors about ownership or modes usually concern home, `.ssh`, or `authorized_keys` path components.
- A connection reaching the banner but failing later proves the port and daemon are reachable.
- Use server and client debug logs together for a complete timeline.

## 8. L3 troubleshooting workflow

1. **Define impact:** Identify the affected service, users, start time, scope, and business symptom.
2. **Preserve evidence:** Capture relevant logs, configuration, counters, timestamps, versions, and current state.
3. **Confirm ownership:** Determine whether systemd, NetworkManager, authselect, firewalld, policy, automation, or another manager owns the configuration.
4. **Use read-only checks first:** Avoid changing state before the failure mode is understood.
5. **Correlate adjacent layers:** Check process, kernel, filesystem, network, DNS, time, identity, audit, and SELinux evidence as appropriate.
6. **Form one hypothesis:** State what evidence supports it and what observation would disprove it.
7. **Make one controlled change:** Use a maintenance window, backup, console path, and rollback command.
8. **Validate service metrics:** Confirm application latency, error rate, authentication, data integrity, and logs—not only command success.
9. **Restore or persist:** Roll back failed experiments and make successful changes through approved configuration management.
10. **Document RCA:** Record root cause, contributing factors, corrective action, prevention, and monitoring.

## 9. Production cautions

- Keep an existing administrative session and console path during SSH changes.
- Validate with `sshd -t` before reload.
- Do not disable all working authentication methods before proving the replacement.
- Do not regenerate host keys casually; clients will see a changed server identity.
- Never copy example users, paths, ports, addresses, or devices into production without validation.
- Commands that change credentials, access, labels, firewall rules, packages, audit rules, or services require change control.
- Keep an independent administrative path for SSH, PAM, firewall, DNS, and authentication work.

## 10. Quick practice lab

1. Copy the configuration to an isolated VM.
2. Validate and print the effective configuration.
3. Run a debug daemon on port 2222.
4. Test one successful and one denied login.
5. Review client and server debug output.

## 11. Evidence collection template

```text
Host:
Date/time and timezone:
Kernel and OS release:
Affected service/application:
Business symptom:
Command executed:
Complete output:
Exit status:
Related logs:
Before state:
Change made:
After state:
Rollback result:
Root cause:
Corrective and preventive action:
```

## 12. L3 interview review

- Explain what kernel interface, service, database, or configuration source the command uses.
- Distinguish read-only inspection from a runtime or persistent change.
- Explain at least two misleading interpretations of the output.
- State the correct rollback before proposing a production change.
- Describe how automation or a service manager can overwrite a manual setting.
- Identify the security, availability, and data-integrity risks.
